/**
 * 04_operations.js
 *
 * This file demonstrates basic arithmetic operations in JavaScript.
 */

// Addition

/**
 * Adds two numbers and prints the result.
 */
var sum = 124 + 55; // Addition
console.log(sum); // Prints the sum

// Subtraction

/**
 * Subtracts one number from another and prints the result.
 */
var difference = 145 - 5; // Subtraction
console.log(difference); // Prints the difference

// Multiplication

/**
 * Multiplies two numbers and prints the result.
 */
var product = 12 * 5; // Multiplication
console.log(product); // Prints the product

// Division

/**
 * Divides one number by another and prints the result.
 */
var quotient = 56 / 6; // Division
console.log(quotient); // Prints the quotient

// Finding Remainder

/**
 * Calculates the remainder of dividing one number by another and prints the result.
 */
var remainder;
/**
 * The remainder operator (%) returns the remainder of dividing one number by another.
 * The result is a whole number, and the sign of the result is the same as the sign of the dividend.
 */
remainder = 19 / 4;
console.log(remainder); // Prints the remainder

